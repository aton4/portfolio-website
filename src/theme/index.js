import { createTheme } from '@mui/material/styles'

let theme = createTheme({
  palette: {
    primary: {
      main: '#ffffff',
      bgColor: '#2d3748',
      text: '#ffffff',
    },
    secondary: {
      main: '#ffffff',
      bgColor: '#2d3748',
      text: '#cccccc',
    },
  },
})

export default theme
