import { createTheme } from '@mui/material/styles'

let theme = createTheme({
  palette: {
    primary: {
      main: '#2d3748',
      bgColor: '#2d3748',
      text: 'white',
    },
    secondary: {
      main: '#2d3748',
      bgColor: '#2d3748',
      text: '#cccccc',
    },
  },
})

export default theme
